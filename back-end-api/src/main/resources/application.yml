server:
  port: 8000

spring:
  application:
    name: entertainment-ticketing-system
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: yueying
    password: yueying@Admin123
    url: jdbc:mysql:// rm-2zel1un0a2xa61n7hlo.mysql.rds.aliyuncs.com:3306/db_yueying

mybatis-plus:
  global-config:
    db-config:
      logic-delete-field: flag # 全局逻辑删除的实体字段名(since 3.3.0,配置后可以忽略不配置步骤2)
      logic-delete-value: 1 # 逻辑已删除值(默认为 1)
      logic-not-delete-value: 0 # 逻辑未删除值(默认为 0)
